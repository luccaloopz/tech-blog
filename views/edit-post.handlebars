{{!-- this will be a card with the title of the post (within a text field), the ... --}}
{{!-- ... content of the post (within a text field), an update button that... --}}
{{!-- ... brings the user back to the now updated dashboard, and a delete button that ... --}}
{{!-- ... brings the user back to the now updated dashboard with the post no longer there --}}